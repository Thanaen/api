pre-commit:
  commands:
    oxlint:
      glob: "**/*.{js,jsx,ts,tsx}"
      run: bun run lint:fix {staged_files}
      stage_fixed: true
    oxfmt:
      glob: "*"
      run: bun run fmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
